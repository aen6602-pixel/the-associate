# 버전 고정: 클라우드 배포가 로컬과 동일하게 빌드되도록

# 웹 서버 (UI 는 web/ 의 정적 파일 + fetch/SSE — 프론트엔드 빌드 스텝 없음)
fastapi==0.141.1
uvicorn[standard]==0.52.4

# 에이전트 두뇌
google-genai==2.17.0    # Gemini (무료티어, 로컬 기본)
openai==2.53.0          # GPT (배포 기본 두뇌). DeepSeek 도 이 패키지 재사용(base_url 만 다른 OpenAI 호환 API)
anthropic==0.120.2      # Claude (로컬 CLI 경로용, 배포에선 미사용)

# Market Muse — 텔레그램 채널 수집(MTProto).
# 봇 토큰으로는 채널 글을 못 읽어(봇은 자신이 속한 대화만 본다) 사람 계정 세션을 쓴다.
telethon==1.36.0

# HTTP / 파싱
requests==2.34.2
truststore==0.10.4      # 사내 TLS 검사 환경용. 클라우드에선 불필요하나 config 의 try/except 로 무해
python-dotenv==1.2.2
lxml==6.1.1
beautifulsoup4==4.15.0

# 데이터 처리 & 엑셀
pandas==2.3.3
openpyxl==3.1.5        # .xlsx (Damodaran ctryprem)
xlrd==2.0.2            # .xls (Damodaran 산업별 베타/WACC 데이터셋은 레거시 xls)

# 시장 데이터 (키 불필요) — 무거워서 별도 설치. 현재 코드 경로에선 미사용(네이버 우회 사용)
# pykrx        # 한국 주가/시총
# yfinance     # 미/일/대만 주가
